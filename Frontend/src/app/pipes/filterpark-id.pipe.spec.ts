import { FilterparkIDPipe } from './filterpark-id.pipe';

describe('FilterparkIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterparkIDPipe();
    expect(pipe).toBeTruthy();
  });
});
