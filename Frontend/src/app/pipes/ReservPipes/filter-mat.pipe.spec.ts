import { FilterMatPipe } from "app/pipes/ReservPipes/filter-mat.pipe";

describe('FilterMatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMatPipe();
    expect(pipe).toBeTruthy();
  });
});
