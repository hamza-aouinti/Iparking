import { FilterPlaceNamePipe } from './filter-place-name.pipe';

describe('FilterPlaceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlaceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
