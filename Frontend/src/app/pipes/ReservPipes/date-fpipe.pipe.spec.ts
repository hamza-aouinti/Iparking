import { DateFpipePipe } from './date-fpipe.pipe';

describe('DateFpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFpipePipe();
    expect(pipe).toBeTruthy();
  });
});
