import { TimeDpipePipe } from './time-dpipe.pipe';

describe('TimeDpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDpipePipe();
    expect(pipe).toBeTruthy();
  });
});
