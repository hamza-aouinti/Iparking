import { TimeFpipePipe } from './time-fpipe.pipe';

describe('TimeFpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFpipePipe();
    expect(pipe).toBeTruthy();
  });
});
