import { FilterParkPipe } from './filter-park.pipe';

describe('FilterParkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParkPipe();
    expect(pipe).toBeTruthy();
  });
});
