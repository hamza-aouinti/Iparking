import { FilterparkNamePipe } from './filterpark-name.pipe';

describe('FilterparkNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterparkNamePipe();
    expect(pipe).toBeTruthy();
  });
});
